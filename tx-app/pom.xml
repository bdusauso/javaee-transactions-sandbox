<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>eu.codinsanity.sandbox</groupId>
        <artifactId>transaction-sandbox</artifactId>
        <version>DEV-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>transaction-sandbox-app</artifactId>
    <packaging>ear</packaging>
    <name>${project.appName} : EAR</name>
    <version>${project.parent.version}</version>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>transaction-sandbox-rest</artifactId>
            <version>${project.parent.version}</version>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources/META-INF</directory>
                <includes>
                    <include>weblogic-application.xml</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ear-plugin</artifactId>
                <configuration>
                    <defaultLibBundleDir>APP-INF/lib</defaultLibBundleDir>
                    <skinnyWars>true</skinnyWars>
                    <fileNameMapping>full</fileNameMapping>
                    <modules>
                        <webModule>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>transaction-sandbox-rest</artifactId>
                            <contextRoot>/sandbox</contextRoot>
                        </webModule>
                    </modules>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>deploy-local-docker</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.oracle.weblogic</groupId>
                        <artifactId>weblogic-maven-plugin</artifactId>
                        <version>12.1.2-0-0</version>
                        <dependencies>
                            <dependency>
                                <groupId>weblogic</groupId>
                                <artifactId>wldescriptor</artifactId>
                                <version>1.3.0</version>
                            </dependency>
                            <dependency>
                                <groupId>weblogic</groupId>
                                <artifactId>weblogic</artifactId>
                                <version>10.3</version>
                            </dependency>
                            <dependency>
                                <groupId>com.bea.weblogic</groupId>
                                <artifactId>wlfullclient</artifactId>
                                <version>10.3.1.0</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <adminurl>t3://localhost:7272</adminurl>
                            <upload>true</upload>
                            <remote>true</remote>
                            <verbose>true</verbose>
                            <debug>true</debug>
                            <failOnError>true</failOnError>
                            <user>weblogic</user>
                            <password>weblogic1</password>
                            <source>${project.build.directory}/${project.build.finalName}.${project.packaging}</source>
                            <name>${project.build.finalName}.${project.packaging}</name>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>install</phase>
                                <goals>
                                    <goal>redeploy</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
